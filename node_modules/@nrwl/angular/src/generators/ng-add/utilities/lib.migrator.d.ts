import { Tree } from '@nrwl/devkit';
import { GeneratorOptions } from '../schema';
import { Logger } from './logger';
import { ProjectMigrator } from './project.migrator';
import { MigrationProjectConfiguration, ValidationResult } from './types';
declare type SupportedTargets = 'build' | 'test' | 'lint';
export declare class LibMigrator extends ProjectMigrator<SupportedTargets> {
    private oldEsLintConfigPath;
    private newEsLintConfigPath;
    constructor(tree: Tree, options: GeneratorOptions, project: MigrationProjectConfiguration, logger?: Logger);
    migrate(): Promise<void>;
    validate(): ValidationResult;
    private moveProjectFiles;
    private updateProjectConfiguration;
    private updateNgPackageJson;
    private updateTsConfigs;
    private updateEsLintConfig;
    private updateBuildTargetConfiguration;
    private updateLintTargetConfiguration;
    private updateTestTargetConfiguration;
    private updateTsConfigFileUsedByBuildTarget;
    private updateTsConfigFileUsedByTestTarget;
}
export {};
