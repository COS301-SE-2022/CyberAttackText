import type { Tree } from '@nrwl/devkit';
import type { Schema } from '../schema';
export declare function generateWebpackConfig(host: Tree, options: Schema, appRoot: string, remotesWithPorts: {
    remoteName: string;
    port: number;
}[]): void;
