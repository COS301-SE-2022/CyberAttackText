import { type Compiler, type WebpackPluginInstance } from 'webpack';
import { ExecutorContext, type ProjectGraph } from '@nrwl/devkit';
import type { BuildNodeBuilderOptions } from './types';
export declare class GeneratePackageJsonWebpackPlugin implements WebpackPluginInstance {
    private readonly context;
    private readonly projectGraph;
    private readonly options;
    constructor(context: ExecutorContext, projectGraph: ProjectGraph, options: BuildNodeBuilderOptions);
    apply(compiler: Compiler): void;
}
